import React, {useLayoutEffect} from "react";
import './Checkout.css'

export default function Checkout() {
  useLayoutEffect(() => {
    window.scrollTo(0, 0)
  });

  return (

    <div>
      <h2>Finalizar Compra</h2>
    </div>
  );
}
